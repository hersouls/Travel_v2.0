# .github/workflows/deploy.yml

name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

# ✅ 핵심 권한 수정
permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4@sha256:270f299bd1b1198be61e6d0e2fb95697d6f986d9300d70b0bec8211ff33c9b0c

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Type check
        run: npm run type-check

      - name: Build
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        if: github.ref == 'refs/heads/main'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          cname: oh.moonwave.kr
          force_orphan: true
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'

      - name: ✅ Deploy Status
        if: github.ref == 'refs/heads/main'
        run: |
          echo "🚀 Deployment completed successfully!"
          echo "🌐 Site URL: https://oh.moonwave.kr"
          echo "📅 Deployed at: $(date)"
